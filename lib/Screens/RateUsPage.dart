import 'package:flutter/material.dart';

class RateUsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('RateUsPage'),
        backgroundColor: Colors.green,
      ),
    );
  }
}
